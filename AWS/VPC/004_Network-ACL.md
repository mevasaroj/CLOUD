# 1. Network ACLs (NACLs)
 - An AWS Network Access Control List (NACL) is an optional, stateless security layer for your VPC that acts as a firewall controlling inbound and outbound traffic at the subnet level.
 - NACL is stateless, which must require to configure both inbound and outbound rules to allow return traffic.
